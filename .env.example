# Leave empty when the web server reverse-proxies /api/local to Local Helper.
VITE_LOCAL_HELPER_URL=

# For a direct browser-to-Helper deployment, use an explicit URL at build time.
# The Helper must then bind to a reachable address and allow the exact dashboard Origin.
# VITE_LOCAL_HELPER_URL=http://192.0.2.10:8787
